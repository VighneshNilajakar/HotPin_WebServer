# Termux/Android compatible requirements
# Install with: pip install -r requirements-termux.txt
#
# ⚠️ IMPORTANT: Use Python 3.11 (NOT 3.12)
# Python 3.12 requires Pydantic v2 which needs Rust compilation (fails on Termux)
# Install Python 3.11: pkg install python-3.11
# Then: python3.11 -m venv venv

# SPEECH-TO-TEXT: PocketSphinx
# - Fully offline, lightweight (~30MB)
# - Good accuracy for command recognition
# - Pure Python, no compilation needed
# - Works natively on Termux/Android

# Core web framework (NO Rust compilation needed with Python 3.11)
fastapi==0.68.2
pydantic==1.10.13
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
websockets==12.0
httpx==0.25.2

# Image processing
Pillow==10.1.0

# STT - PocketSphinx (works on Termux!)
pocketsphinx>=5.0.0

# Basic utilities that work on Termux
psutil>=5.9.0
qrcode>=7.4.2

# Note: The following packages are NOT compatible with Termux:
# - pyttsx3 (requires system TTS engines)
# - zeroconf (compilation issues on some Termux versions)
# - uvicorn[standard] extras (watchfiles requires Rust)
